import React from "react"
import Subheader from "./subheader"
const Donate = () => (
  <section id="donate">
    <header className="prose">
      <Subheader className="">Donate</Subheader>
    </header>
  </section>
)

export default Donate
