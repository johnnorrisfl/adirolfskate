import React from "react"
import Subheader from "./subheader"

const Merch = () => (
  <section id="merch">
    <header className="prose">
      <Subheader className="prose-headings:h2">Merch</Subheader>
    </header>
  </section>
)

export default Merch
